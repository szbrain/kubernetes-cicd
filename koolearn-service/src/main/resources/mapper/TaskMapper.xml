<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koolearn.club.mapper.TaskMapper">
  <sql id="Base_Column_List">id, class_id, status, `type`, content, resource_ids, create_time, update_time</sql>
  <resultMap id="BaseResultMap" type="com.koolearn.club.entity.PeTask">
      <id column="id" property="id" jdbcType="INTEGER"/>
      <result column="class_id" property="classId" jdbcType="INTEGER"/>
      <result column="status" property="status" jdbcType="TINYINT"/>
      <result column="type" property="type" jdbcType="TINYINT"/>
      <result column="content" property="content" jdbcType="VARCHAR"/>
      <result column="resource_ids" property="resourceIds" jdbcType="VARCHAR"/>
      <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
      <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
  </resultMap>

    <select id="getById" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_task
        where id=#{id} and is_delete != 1
    </select>

    <select id="getLastTaskByClassId" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_task
        where class_id = #{classId} and is_delete != 1 and type = 1
        order by create_time desc
        limit 1
    </select>

    <select id="getLastTaskByClassIdOfV2" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_task
        where class_id = #{classId} and is_delete != 1
        order by create_time desc
        limit 1
    </select>


    <select id="listByClassId" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_task
        where class_id = #{classId} and is_delete != 1 and type = 1
        order by create_time desc
        limit #{offset},#{pageSize}
    </select>

    <select id="listByClassIdOfV2" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_task
        where class_id = #{classId} and is_delete != 1
        order by create_time desc
        limit #{offset},#{pageSize}
    </select>


    <select id="countByClassId" resultType="java.lang.Integer">
        select count(id) from pe_task
        where class_id = #{classId} and is_delete != 1 and type = 1
    </select>

    <select id="countByClassIdOfV2" resultType="java.lang.Integer">
        select count(id) from pe_task
        where class_id = #{classId} and is_delete != 1
    </select>


    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.koolearn.club.entity.PeTask">
        insert into pe_task(
        status, `type`, class_id, content, resource_ids, create_time
        )
        values(
        #{status}, #{type}, #{classId}, #{content}, #{resourceIds}, #{createTime}
        )
    </insert>


    <select id="listAllByClassId" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_task
        where class_id = #{classId} and is_delete != 1
        order by create_time desc
    </select>



    <update id="update" parameterType="com.koolearn.club.entity.PeTask">
        update pe_task
        set content = #{content},
        resource_ids = #{resourceIds}
        where id=#{id}
    </update>

</mapper>
