<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koolearn.club.mapper.StudentSettingMapper">

    <sql id="Base_Column_List">id, stu_id, class_id, remind_time, create_time, update_time</sql>

  <resultMap id="BaseResultMap" type="com.koolearn.club.entity.PeStudentSetting">
      <id column="id" property="id" jdbcType="INTEGER"/>
      <result column="stu_id" property="stuId" jdbcType="INTEGER"/>
      <result column="class_id" property="classId" jdbcType="INTEGER"/>
      <result column="remind_time" property="remindTime" jdbcType="INTEGER"/>
      <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
      <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
  </resultMap>

    <select id="getById" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_student_setting
        where id=#{id} and is_delete != 1
    </select>

    <select id="getListHaveRemindTime" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_student_setting
        where  remind_time > 0 and is_delete != 1
    </select>

    <select id="getByStuIdClassId" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_student_setting
        where stu_id=#{stuId} and class_id=#{classId} and is_delete != 1
    </select>


    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.koolearn.club.entity.PeStudentSetting">
        insert into pe_student_setting(
        stu_id, class_id, remind_time, create_time
        )
        values(
        #{stuId}, #{classId}, #{remindTime}, #{createTime}
        )
    </insert>


    <update id="update" parameterType="com.koolearn.club.entity.PeStudentSetting">
        update pe_student_setting
        set remind_time = #{remindTime}
        where id=#{id}
    </update>

</mapper>
