<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koolearn.club.mapper.ClassStatMapper">
  <sql id="Base_Column_List">id, class_id, `count`, stat_time, task_undone_count, join_count, quit_count, create_time</sql>
  <sql id="Relation_Column_List">a.id, a.class_id, a.`count`, a.stat_time, a.task_undone_count, a.join_count, a.quit_count, a.create_time</sql>

  <resultMap id="BaseResultMap" type="com.koolearn.club.entity.PeClassStat">
      <id column="id" property="id" jdbcType="INTEGER"/>
      <result column="class_id" property="classId" jdbcType="INTEGER"/>
      <result column="count" property="count" jdbcType="INTEGER"/>
      <result column="stat_time" property="statTime" jdbcType="TIMESTAMP"/>
      <result column="task_undone_count" property="taskUndoneCount" jdbcType="INTEGER"/>
      <result column="join_count" property="joinCount" jdbcType="INTEGER"/>
      <result column="quit_count" property="quitCount" jdbcType="INTEGER"/>
      <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
  </resultMap>

    <select id="getById" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_class_stat
        where id=#{id} and is_delete != 1
    </select>


    <select id="listByClassId" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_class_stat
        where class_id=#{classId} and is_delete != 1
        order by stat_time desc
        limit 10
    </select>


    <select id="listByTeachId" resultMap="BaseResultMap">
        select <include refid="Relation_Column_List"/> from pe_class_stat a
        left join pe_class b on a.class_id = b.id
        where b.teach_id=#{teachId} and a.is_delete != 1 and b.is_delete != 1
        order by b.create_time desc
    </select>


    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.koolearn.club.entity.PeClassStat">
        insert into pe_class_stat(
        class_id, `count`, stat_time, task_undone_count, join_count, quit_count, create_time
        )
        values(
        #{classId}, #{count}, #{statTime}, #{taskUndoneCount}, #{joinCount}, #{quitCount}, #{createTime}
        )
    </insert>


    <select id="getByClassId" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_class_stat
        where class_id=#{classId} and is_delete != 1 and to_days(stat_time) = to_days(#{yesterday})
        limit 1
    </select>


    <select id="allByDateWechatNocie" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from pe_class_stat
        where is_delete != 1 and to_days(stat_time) = to_days(#{yesterday}) and is_wechat_notice != 1
    </select>

    <update id="updateWechatNotice">
        update pe_class_stat
        set is_wechat_notice = 1
        where id=#{id}
    </update>


</mapper>
